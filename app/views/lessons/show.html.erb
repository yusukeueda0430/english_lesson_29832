<%= render "shared/header" %>

<%# 商品の概要 %>
<div class="item-show">
  <div class="item-box">
    <h2 class="name">
      <%= @lesson.name %>
    </h2>
    <div class='item-img-content'>
      <%= image_tag @lesson.image, class:"item-box-img" %>
      <%# 商品が売れている場合は、sold outを表示しましょう %>
      <%# <% if @lesson.order%>
       <%# <div class='sold-out'>
        <span>Sold Out!!</span>
       </div> 
      <% end %>
      <%# //商品が売れている場合は、sold outを表示しましょう %>
    </div>
    <div class="item-price-box">
      <span class="item-price">
        <%= @lesson.price %>
      </span>
      <span class="item-postage">
        (税込)
      </span>
    </div>

    <%# ログインしているユーザーと出品しているユーザーが、同一人物の場合と同一人物ではない場合で、処理を分けましょう %>
    <% if teacher_signed_in? && current_teacher.id == @lesson.teacher_id %>
      <%= link_to '商品の編集', edit_lesson_path(@lesson.id), method: :get, class: "item-red-btn" %>
      <p class='or-text'>or</p>
      <%= link_to '削除', lesson_path(@lesson.id), method: :delete, class:'item-destroy' %>
    <% end %>

    <%# 商品が売れていない場合はこちらを表示しましょう %>
    <% if student_signed_in? && @lesson.order == nil %>
      <%= link_to '購入画面に進む', lesson_orders_path(@lesson.id) ,class:"item-red-btn"%>
    <% end %>
    <%# //商品が売れていない場合はこちらを表示しましょう %>


    <%# //ログインしているユーザーと出品しているユーザーが、同一人物の場合と同一人物ではない場合で、処理を分けましょう %>

    <div class="item-explain-box">
      <span><%= @lesson.details %></span>
    </div>
    <table class="detail-table">
      <tbody>
        <tr>
          <th class="detail-item">出品者</th>
          <td class="detail-value"><%= @lesson.teacher.nickname %></td>
        </tr>
        <tr>
          <th class="detail-item">カテゴリー</th>
          <td class="detail-value"><%= @lesson.category.name %></td>
        </tr>
        <tr>
          <th class="detail-item">居住地</th>
          <td class="detail-value"><%= @lesson.living_place %></td>
        </tr>
        <tr>
          <th class="detail-item">留学経験</th>
          <td class="detail-value"><%= @lesson.ryugaku_experience.name %></td>
        </tr>
        <tr>
          <th class="detail-item">海外在住歴</th>
          <td class="detail-value"><%= @lesson.overseas_residence_history.name %></td>
        </tr>
        <tr>
          <th class="detail-item">レッスンの回数</th>
          <td class="detail-value"><%= @lesson.number_of_times.name %></td>
        </tr>
        <tr>
          <th class="detail-item">レッスンを実施する曜日</th>
          <td class="detail-value"><%= @lesson.what_day %></td>
        </tr>
        <tr>
          <th class="detail-item">開始時間</th>
          <td class="detail-value"><%= @lesson.start_time.name %></td>
        </tr>
        <tr>
          <th class="detail-item">終了時間</th>
          <td class="detail-value"><%= @lesson.end_time.name %></td>
        </tr>
      </tbody>
    </table>
    <div class="option">
      <div class="favorite-btn">
        <%= image_tag "star.png" ,class:"favorite-star-icon" ,width:"20",height:"20"%>
        <span>お気に入り 0</span>
      </div>
      <div class="report-btn">
        <%= image_tag "flag.png" ,class:"report-flag-icon" ,width:"20",height:"20"%>
        <span>不適切な商品の通報</span>
      </div>
    </div>
  </div>
  <%# /商品の概要 %>

  <div class="comment-box">
    <form>
      <textarea class="comment-text"></textarea>
      <p class="comment-warn">
        相手のことを考え丁寧なコメントを心がけましょう。
        <br>
        不快な言葉遣いなどは利用制限や退会処分となることがあります。
      </p>
      <button type="submit" class="comment-btn">
        <%= image_tag "comment.png" ,class:"comment-flag-icon" ,width:"20",height:"25"%>
        <span>コメントする<span>
      </button>
    </form>
  </div>
  <div class="links">
    <a href="#" class="change-item-btn">
      ＜ 前の商品
    </a>
    <a href="#" class="change-item-btn">
      後ろの商品 ＞
    </a>
  </div>
  <a href="#" class='another-item'>カテゴリー「<%= @lesson.category.name %>」をもっと見る</a>
</div>

<%= render "shared/footer" %>
